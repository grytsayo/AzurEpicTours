Run the following git checks. Do not modify any files.

1. Run `git fetch origin` to update remote tracking refs.
2. Run `git status` and report:
   - Whether local branch is behind, ahead, or in sync with remote
   - List of modified tracked files (if any)
   - List of untracked files (if any)
3. Run `git log --oneline -5` and show the last 5 commits.

After running all three commands, summarize in plain text:
- "Up to date" or "Behind by N commits — run git pull before editing"
- Modified files: <list or "none">
- Untracked files: <list or "none">
